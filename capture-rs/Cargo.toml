[package]
name = "xdesk-capture"
version = "0.1.0"
edition = "2021"

[dependencies]
windows = { version = "0.52", features = [
    "Win32_Graphics_Gdi",
    "Win32_Foundation",
] }
image = { version = "0.25", features = ["jpeg"] }

[profile.release]
opt-level = 3
lto = true
strip = true
